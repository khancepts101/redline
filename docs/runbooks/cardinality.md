# Cardinality explosion
Find the leading metric in the Cardinality Guard dashboard, identify its unbounded label, and temporarily drop the offending series with `metric_relabel_configs` before fixing the producer.
